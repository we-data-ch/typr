mod animation;
use animation::Animation;

@pub
type Animable <- interface {
	animate: (self: Self, animation: Animation) -> Self
};

@pub
type Id <- list { id: char };

@pub
type Object <- Id & Animable;
