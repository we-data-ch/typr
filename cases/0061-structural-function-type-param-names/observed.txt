# Claimed in comp_correction.md (section B, 2026-09-09 audit of the
# `noplayground` doc blocks against 0.5.10) -- NOT reproduced when tested
# against current source (0.5.10, develop @ 90f6e30). Kept here verbatim as
# the reported symptom, for reference:

Type error: The output type of the function don't match it's type annotation
Expected: fn(a: int) -> int
Found:    fn(z: int) -> int

# Actual output on this repro (current source):
#
#   Type checking... done
#   File verification "main.ty" successful!
