## Should happen

Same as `0044-foreign-s3-record-field`, for the base-attributed-vector row:
`w.model` must show the real ordered-`factor` print output (`Levels: ...`).

## Localisation (#@case)

Part of Phase D.4 of `soundness_plan.md`.
