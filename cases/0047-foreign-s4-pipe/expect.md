## Should happen

Same as `0045-foreign-s3-pipe`, for the S4 row: `m |> print()` must show
the real `Matrix::Matrix` print output.

## Localisation (#@case)

Part of Phase D.4 of `soundness_plan.md`.
