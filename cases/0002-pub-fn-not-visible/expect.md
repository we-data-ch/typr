I have made a public function `do` but it doesn't seem to exist into the R code génerated in the `R/` folder. Do I miss something while running `typr run`. It works for `Object:{ a: 1, b: false }` though

## Localisation (#@case)

- `TypR/main.ty:3` — : public function imported
- `TypR/truc.ty:7` — : public function defined
