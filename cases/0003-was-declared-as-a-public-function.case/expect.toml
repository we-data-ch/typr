# Circle's S3 class vector must include its cross-module structural
# supertype Position, so `move` (declared only for Position) dispatches.
[[rule]]
file = "R/circle.R"
must_contain = "class(x) <- c(\"Circle\", \"Position\", \"Record0\", \"list\")"
