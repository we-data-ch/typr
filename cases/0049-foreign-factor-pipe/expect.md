## Should happen

Same as `0045-foreign-s3-pipe`, for the base-attributed-vector row:
`m |> print()` must show the real ordered-`factor` print output.

## Localisation (#@case)

Part of Phase D.4 of `soundness_plan.md`.
