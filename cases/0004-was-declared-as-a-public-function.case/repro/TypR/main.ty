mod circle;
mod position;
mod scene;

use circle::new_circle;
use circle::Circle;
use position::Position;
use position::move;
use position::right;
use scene::Scene;
use scene::new_scene;


let c1 <- new_circle(2);

c1.print()
