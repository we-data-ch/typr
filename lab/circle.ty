mod color;

type Circle <- list {
	center: Vec[2, int],
	radius: int,
	stroke: Color,
	fill: Color
};

let new_circle <- fn(r: int): Circle {
	Circle:{
		center: c(0, 0),
		radius: r,
		stroke: "#000000",
		fill: "#ffffff"
	}
};

Test {
	test_that("circle with the right radius", {
		let c1 <- new_circle(4);
		expect_equal(c1$radius, 4)
	})
}
